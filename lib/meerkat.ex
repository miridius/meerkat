defmodule Meerkat do
  @moduledoc """
  Local diff reviewer. The CLI parses args, reads the staged diff,
  and either auto-approves an empty change-set or starts a Phoenix
  server backing a LiveView review UI. See `Meerkat.CLI`.
  """
end
